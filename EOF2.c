/*Accept characters till the user enters EOF and count the number of lines entered. Also 
display the length of the longest line. (Hint: A line ends when the chara*/
#include<stdio.h>
int main()
{
    char ch;
    while((ch=getchar())!=EOF)
    
}