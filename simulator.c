#include<stdio.h>
int main()
{
       int a[10],n,i,j;
       printf("Enter how many elements\n");
       scanf("%d",&n);
       printf("Enter elements\n");
       for(i=0;i<n;i++)
       {
              scanf("%d",&a[i]);
       }

       for(i=0;i<ni++)
       {
              if(a[i]>a[i+1])
              {
                     a[i]=a[i+1];
                     

              }
       }
}